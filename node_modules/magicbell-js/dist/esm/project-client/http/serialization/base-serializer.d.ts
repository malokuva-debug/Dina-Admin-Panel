import { RequestParameter } from '../transport/types.js';
export type SerializationOptions = {
    explode: boolean;
    style: SerializationStyle;
    encode: boolean;
};
export declare enum SerializationStyle {
    SIMPLE = "simple",
    LABEL = "label",
    MATRIX = "matrix",
    FORM = "form",
    SPACE_DELIMITED = "space_delimited",
    PIPE_DELIMITED = "pipe_delimited",
    DEEP_OBJECT = "deep_object",
    NONE = "none"
}
export declare class Serializer {
    protected serializeValue(param: RequestParameter): string;
    private serializePrimitive;
    private serializeArray;
    private serializeArrayExploded;
    private serializeObject;
    private isNonNullObject;
}
//# sourceMappingURL=base-serializer.d.ts.map