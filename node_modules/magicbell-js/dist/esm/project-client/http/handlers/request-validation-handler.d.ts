import { Request } from '../transport/request.js';
import { HttpResponse, RequestHandler } from '../types.js';
export declare class RequestValidationHandler implements RequestHandler {
    next?: RequestHandler;
    handle<T>(request: Request): Promise<HttpResponse<T>>;
    stream<T>(request: Request): AsyncGenerator<HttpResponse<T>>;
    validateRequest(request: Request): void;
    toFormUrlEncoded(request: Request): string;
    toFormData(body: Record<string, any>, filename?: string, filenames?: string[]): FormData;
}
//# sourceMappingURL=request-validation-handler.d.ts.map