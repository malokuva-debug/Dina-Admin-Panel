import { HttpError } from '../hooks/hook.js';
import { HttpResponse } from '../types.js';
import { Request } from './request.js';
export declare class TransportHookAdapter<T> {
    private hook;
    beforeRequest(request: Request, params: Map<string, string>): Promise<Request>;
    afterResponse(request: Request, response: HttpResponse<T>, params: Map<string, string>): Promise<HttpResponse<T>>;
    onError(request: Request, response: HttpResponse<T>, params: Map<string, string>): Promise<HttpError>;
    private requestToHookRequest;
    private hookParamsToTransportParams;
}
//# sourceMappingURL=transport-hook-adapter.d.ts.map