export declare class LineDecoder {
    private lineBuffer;
    private decoder;
    private encoder;
    /**
     * Splits the given chunk into lines.
     * Stores incomplete lines in a buffer and returns them when the next chunk arrives.
     */
    splitLines(chunk: Uint8Array): Uint8Array[];
    /** Returns the remaining lines in the buffer. */
    flush(): Uint8Array[];
}
//# sourceMappingURL=line-decoder.d.ts.map