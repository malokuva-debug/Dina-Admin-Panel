import { RequestFetchAdapter } from '../transport/request-fetch-adapter.js';
export class TerminatingHandler {
    async handle(request) {
        return new RequestFetchAdapter(request).send();
    }
    async *stream(request) {
        yield* new RequestFetchAdapter(request).stream();
    }
}
//# sourceMappingURL=terminating-handler.js.map