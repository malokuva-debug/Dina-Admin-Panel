import { Hook } from '../hooks/hook.js';
import { Request } from '../transport/request.js';
import { HttpResponse, RequestHandler } from '../types.js';
export declare class HookHandler implements RequestHandler {
    private readonly hook;
    next?: RequestHandler;
    constructor(hook: Hook);
    handle<T>(request: Request): Promise<HttpResponse<T>>;
    stream<T>(request: Request): AsyncGenerator<HttpResponse<T>>;
    private getHookParams;
}
//# sourceMappingURL=hook-handler.d.ts.map