import { HttpResponse } from '../types.js';
import { Request } from './request.js';
interface HttpAdapter {
    send(): Promise<HttpResponse>;
    stream(): AsyncGenerator<HttpResponse>;
}
export declare class RequestFetchAdapter<T> implements HttpAdapter {
    private request;
    private requestInit;
    constructor(request: Request);
    send(): Promise<HttpResponse<T>>;
    stream(): AsyncGenerator<HttpResponse<T>>;
    private setMethod;
    private setBody;
    private setHeaders;
    private setTimeout;
    private getHeaders;
    private toArrayBuffer;
}
export {};
//# sourceMappingURL=request-fetch-adapter.d.ts.map